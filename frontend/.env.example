# Use /api for local development (this works with the Vite proxy)
# For production deployments, change this to the absolute URL of your backend (e.g., https://my-backend.com/api)
VITE_API_BASE_URL=/api
